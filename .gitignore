.DS_Store

node_modules/
klima.db
klima.db-shm
klima.db-wal
klima.db-journal
klima__.db
klima.backup-*.db
.env

# Monthly CSV/ZIP backup output (default backup_dir = <DB dir>/backups in dev; ProgramData in prod)
backups/

node_modules

# Superpowers brainstorming visual-companion mockups (transient)
.superpowers/
